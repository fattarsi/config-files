[Service]
# Strip X11-era scaling env vars before niri starts. niri's per-output
# `scale` is the only scaling we want. Without this, Slack/Electron renders
# 2× too big and Firefox under XWayland gets the wrong DPI.
UnsetEnvironment=GDK_SCALE GDK_DPI_SCALE ELECTRON_FORCE_DEVICE_SCALE_FACTOR
