# App colors for the bar minimap.
# Format: <app-id>=<hex color>
# Use lowercase app-ids. Find your app-ids in niri by running:
#   niri msg --json windows | jq -r '.[].app_id' | sort -u
#
# Lines starting with # are comments. Blank lines are ignored.
# `default` is the fallback color for apps not listed.

firefox=#e07a3a
firefox_firefox=#e07a3a
slack=#a050c0
kitty=#5fb86b
code=#4488dd
google-chrome=#dd5555
chromium=#dd5555
thunderbird=#5588dd
spotify=#1db954
discord=#5865f2

default=#888888
